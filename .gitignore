node_modules/
dist/
*.tsbuildinfo

# Stryker
reports/
.stryker-tmp/

# Terraform
infra/build/
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfplan
*.plan
crash.log
crash.*.log
*.tfvars
!example.tfvars
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc
# .terraform.lock.hcl is intentionally committed: it pins provider hashes
# so CI and every contributor resolve the exact same provider builds.

# Local env
.env
.env.*
!.env.example

# OS
.DS_Store
